## 什么是作用域？

作用域是代码运行时此环境下可以访问的变量，函数和对象的可访问性

简单来说就是作用域决定了当前环境下的资源是否可以被使用和引用

举例个例子

```javascript
function region() {
  let obj = {};
}
region();
console.log(obj); // 访问不到
```

obj 对象在函数作用域内声明，无法在全局作用域内进行访问

**作用域就相当于是一个独立的地盘，你在这个地盘内定义的变量，函数，对象等无法被外部访问，可以隔离变量，不同作用域下的同名变量也不会有冲突**

> ES6 之前 JavaScript 没有块级作用域，只有全局作用域和函数作用域
>
> ES6 之后提供了"块级作用域"， 通过 let 和 const 来体现

## 全局作用域和函数作用域

### 1，全局作用域

在代码中的任何层级中都能访问到的变量和对象是身处全局作用域中，反而言之全局作用域中的对象和变量应该是无论在代码的任何作用域中都能访问到的

- 最外层的函数和最外层的变量可以在任何地方被访问到，身处全局作用域下

```javascript
let obj = "全局作用域"; //最外层变量
function region() {
  //最外层函数
  console.log(obj); //全局作用域
  function variable() {
    //内层函数
    console.log(obj); //全局作用域
  }
  variable();
}

region();
variable(); //variable  is not defined
```

- 所有未定义直接赋值的变量自动声明为全局作用域下

```javascript
function region() {
  obj = "作用域";
  let obj1 = "函数作用域";
}

console.log(obj); //作用域
console.log(obj); // obj1 is not defined
```

- 所有 window 对象下的属性都是身处在全局作用域下

在一般情况下，window 对象内置的一些属性都是作用在全局作用域下，列如 window.name ，window.location，window.top 等等。

全局作用域有一个问题是如果我们定义了很多全局变量，那么他会污染我们的全局作用域，导致命名冲突

当我们的代码页面过于长，前后代码由不同的人编写时，就会出现这种情况

```javascript
var name = '张三'

....... //数千行代码

var name = '李四'
```

### 2，函数作用域

函数作用域是指在函数内部声明的变量和函数只能在本函数内进行访问和使用，外部无法进行访问和使用

```javascript
function region() {
  let obj = "函数内部变量";
  function name() {
    console.log(obj);
  }
  name();
}

console.log(obj); //obj is not defined
neme(); //neme is not defined
```

有一点需要去注意：块语句（大括号‘{ }’中间的语句），比如 if 条件语句和 for 循环语句等不像函数，他们不会创建一个新的作用域

```javascript
if (true) {
  //if条件语句不会创建一个新的作用域
  var name = "李四"; //注意这个时候这个name依然是全局作用中的
}

console.log(name); // 李四
```

正因为这样 ES6 引入了块级作用域的概念，让变量的生命周期更加可控

## 块级作用域

块级作用域通过 let 和 const 进行声明产生，所声明的变量无法在此作用块语句之外被访问

块级作用域使用产生场景：

1. 在一个函数内部
2. 在块语句（大括号 { } ）之中

```javascript
function region() {
  let age = 19;
  console.log(age); // 19
}

if (true) {
  let name = "李四";
  console.log(name); // 李四
}

console.log(age); // age is not defiend
console.log(name); // name is not defiend
```

#### 特点

- let const 变量不会被变量声明提升，所以一定要定义在你使用变量之前 否则就会报错，一般会放置在作用域顶部
- 禁止重复声明，不会像 var 一样变量声明提升会覆盖之前的赋值，如果该作用域内已经声明了 let 变量，再次声明相同的变量会导致报错
- 在循环内可以解决 var 声明变量的提升到全局作用域问题 ，let 会根据块级作用域生成不同的变量

## 作用域链

#### 什么是自由变量？

如下面代码所示，我要访问变量 name，但是变量 name 不在当前 c 作用域链中定义，那么这就是一个自由变量，

自由变量的访问需要向上级作用域层层查找

**举个例子**

```javascript
let name = "张三";
function a() {
  function b() {
    function c() {
      console.log(name); //张三
    }
  }
}
```

从上面那个例子我们可以看到，我们去访问 name 变量的时候首先去去 C 函数作用域中找，C 没有再去上层作用域 b 找，b 没有再去上上次作用域 a 找，直到我们的全局作用域，这样的一个链式的访问方式被称之为**作用域链**

---

#### **自由变量的取值**

在一些特殊的情况下自由变量的取值会有些不同

```javascript
var c = 10;
function name() {
  console.log(c);
}

function fn(n) {
  var c = 20(
    (function () {
      n(); // n()的结果不是20 而是10
    })()
  );
}

fn(name);
```

在这样的场景下，**我们要确定的是函数创建时的作用域上下级，无论函数调用时在哪里执行，函数内部的代码去获取变量时内部没有的情况下都是根据创建时的作用域上下级向上去查找，而不是函数调用时的作用域**

---

### 作用域链和执行上下文的区别

<font style="color:rgb(77, 77, 77);">JavaScript 属于解释型语言，JavaScript 的执行分为：解释和执行两个阶段,这两个阶段所做的事并不一样</font>

<font style="color:rgb(77, 77, 77);">解释阶段：</font>

- <font style="color:rgb(77, 77, 77);">词法分析</font>
- <font style="color:rgb(77, 77, 77);">语法分析</font>
- <font style="color:rgb(77, 77, 77);">作用域规则确定</font>

<font style="color:rgb(77, 77, 77);">执行阶段：</font>

- <font style="color:rgb(77, 77, 77);">创建执行上下文</font>
- <font style="color:rgb(77, 77, 77);">执行函数代码</font>
- <font style="color:rgb(77, 77, 77);">垃圾回收</font>

**作用域是在解释阶段就已经确定了的，并且是不会改变的是静态的，执行上下文是运行阶段确定的，随时会改变**
